"""
Figure 5A — Step 1: raw simulation data for strategy prediction.

Two large simulation sweeps are required:

  large_strategy_sweep_100.pkl  — strategy sweep over 100 NK landscapes
                                   shape: (100, 7, 7, 300)
                                   generated by: scripts/large_strategy_sweep.py

  large_decay_curve_sweep.pkl   — decay curve sweep over 100 NK landscapes
                                   shape: (100, 25, 10, 25)
                                   generated by: scripts/large_decay_curve_sweep.py

Both files are expected in the SLIDE_data directory
(see slide_config.get_slide_data_dir()).

Usage
-----
  Run the two sweep scripts on a GPU before proceeding to step 2:

    python scripts/large_strategy_sweep.py
    python scripts/large_decay_curve_sweep.py

Shared raw data: none (unique to Fig 5A).
"""

print("Raw data needed:")
print("  SLIDE_data/large_strategy_sweep_100.pkl  (scripts/large_strategy_sweep.py)")
print("  SLIDE_data/large_decay_curve_sweep.pkl   (scripts/large_decay_curve_sweep.py)")
