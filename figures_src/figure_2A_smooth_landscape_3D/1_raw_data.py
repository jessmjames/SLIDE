"""
Figure 2A — Step 1: raw data.

This figure requires no simulation data or pkl files.
The smooth landscape is defined analytically:

    smooth_func(x, y) = sin(y / 3)

evaluated on a 4×4 discrete grid (N=4) and a fine continuous grid (101×101 points).
All data is generated inline in 2_process_data.py and 3_plot.py.

Originally from ruggedness_figures_plots.ipynb cells 84–86.
"""

print("No raw simulation data required for Figure 2A.")
print("The smooth landscape is defined analytically — no pkl files needed.")
